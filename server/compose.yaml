name: podcast_ad_trim

services:
  web:
    build: .
    restart: always
    env_file:
      - .env
    ports:
      - ${HOST_PORT}:80
    volumes:
      - ${STORAGE_PATH}:/home/appuser/storage
      - ${LOG_PATH}:/home/appuser/log